[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ez-pixel"
version = "4.3.68"
description = "Universal error tracking pixel optimized for autonomous coding agents — Python SDK"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Nolan Grout" }]
requires-python = ">=3.8"
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]
# Zero runtime dependencies — uses urllib from stdlib.
dependencies = []

[project.urls]
Homepage = "https://github.com/Gahroot/ezcoder"

[tool.hatch.build.targets.wheel]
packages = ["src/ez_pixel"]

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "README.md"]
