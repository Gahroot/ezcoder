// Moved to @prestyj/core. This shim re-exports it so existing relative
// imports (`./model-registry.js`) and the `@prestyj/cli/models` subpath
// export keep resolving unchanged.
export * from "@prestyj/core/models";
