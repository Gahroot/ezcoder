name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cross-platform smoke matrix. Every push to main and every PR runs build +
# typecheck + test + bin-smoke on macOS, Linux, and Windows so we catch
# platform-specific regressions before they hit npm.
#
# We deliberately keep the gg-editor + ez-pixel packages out of the matrix
# for now — they have heavier native deps (sharp, transformers) that slow CI
# without buying us much for the framework's release-critical packages
# (gg-ai, gg-agent, ezcoder, gg-boss).

jobs:
  test:
    name: ${{ matrix.os }} · node ${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [22.x]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build framework packages
        run: |
          pnpm --filter @prestyj/ai build
          pnpm --filter @prestyj/agent build
          pnpm --filter @prestyj/cli build
          pnpm --filter @prestyj/boss build

      - name: Typecheck
        run: |
          pnpm --filter @prestyj/ai check
          pnpm --filter @prestyj/agent check
          pnpm --filter @prestyj/cli check
          pnpm --filter @prestyj/boss check

      - name: Test framework packages
        run: |
          pnpm --filter @prestyj/ai test
          pnpm --filter @prestyj/agent test
          pnpm --filter @prestyj/cli test
          pnpm --filter @prestyj/boss test

      - name: ezboss CLI smoke (extra belt-and-braces)
        run: node packages/boss/dist/cli.js --help
        shell: bash

  # Distribution-critical cross-OS job: stages the per-platform Node runtime,
  # bundles the sidecar, boots it with the bundled runtime (smoke), and runs the
  # Rust unit tests that gate path/window-chrome resolution. The full
  # `tauri build` lives in release.yml; this keeps CI fast while still
  # exercising the per-OS spawn path that breaks installed bundles.
  app:
    name: app · ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      - name: Linux system deps (tauri)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev librsvg2-dev patchelf libayatana-appindicator3-dev

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build framework packages
        run: |
          pnpm --filter @prestyj/ai build
          pnpm --filter @prestyj/agent build
          pnpm --filter @prestyj/cli build

      - name: Stage node runtime + bundle sidecar
        run: |
          pnpm --filter ezcoder-app stage:node
          pnpm --filter ezcoder-app bundle:sidecar
        shell: bash

      - name: Sidecar smoke (bundled runtime + bundle, per-OS)
        run: node ezcoder-app/scripts/smoke-sidecar.mjs
        shell: bash

      - name: Frontend platform-class test
        run: pnpm --filter ezcoder-app test

      - name: Rust unit tests (path + window-chrome resolution)
        working-directory: ezcoder-app/src-tauri
        run: cargo test
